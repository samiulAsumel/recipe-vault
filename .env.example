# Public site URL used for canonical links, sitemap.xml, and Open Graph images.
# Build-time only (Next.js NEXT_PUBLIC_ vars), not a Cloudflare Worker secret - see
# .dev.vars.example for those. Defaults to the existing Cloudflare Pages domain if unset.
NEXT_PUBLIC_SITE_URL=https://recipevault91.pages.dev
