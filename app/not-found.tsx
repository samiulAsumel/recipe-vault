import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function NotFound(): React.JSX.Element {
  return <PagePlaceholder title="404" description="This page doesn't exist in the atlas yet." />;
}
