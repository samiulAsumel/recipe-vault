import { RouteStub } from '@/components/RouteStub';

export default function NotFound(): React.ReactElement {
  return <RouteStub title="Page not found" path="404" note="This entry doesn't exist in the atlas." />;
}
